// Placeholder foundation route. The real profile-editing feature is a
// later phase — this exists so the shell's navigation has somewhere to go.
export function ProfilePage() {
  return (
    <div className="flex flex-col gap-2">
      <h1 className="text-2xl font-semibold text-foreground">Profile</h1>
      <p className="text-muted-foreground">Profile settings are coming soon.</p>
    </div>
  )
}
