// A full mala — fixed by tradition, never configurable.
export const BEADS_PER_ROUND = 108

// The standard ISKCON minimum daily vow. A default, not a hard limit —
// the counter keeps counting past it, and devotees may change it.
export const DEFAULT_TARGET_ROUNDS = 16
